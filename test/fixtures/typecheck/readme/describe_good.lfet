;; typecheck/readme/describe_good.tlfe
;; C-4: README describe example — EXACT correct version from README.
;; 3
;; 4
;; 5
;; 6
;; 7
(defmodule describe_good
  (export (describe 1)))

(deftype order-status
  (repr tagged-tuple)
  (Pending)
  (Shipped (tracking string))
  (Cancelled (reason string)))

(defun/typed describe
  :args ((status order-status))
  :returns string
  :body (case/typed status
          ((Pending) "queued")
          ((Shipped t) (++ "on its way: " t))
          ((Cancelled r) (++ "cancelled: " r))))
