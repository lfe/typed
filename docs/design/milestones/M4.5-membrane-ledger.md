# Milestone M4.5: The Membrane (Deep Validators & `decode`)

> Per LEDGER_DISCIPLINE.md. CC fills Status/Evidence (commit SHA + reproduced output,
> CI-green); CDC re-verifies. No row stays `open` at close. STANDING RULES
> ([[typed-test-discipline]], [[cc-editing-safety]]): exact `assert_eq!`/snapshots
> (never `.contains()`); test every backend; unwired ≠ done; test the actual subject; no
> blind `sed`. Headline: **D-3** (web-input decode — the motivating example). Split
> render/map-standardization to M4.6 if budget tightens.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| D-1 | **Deep validators (recursive):** `(validate <type> term) -> #(ok term) | #(error type_error)`, generated per declared type, recursive over constructor fields + nested ADTs; base-type validators built in. | Rust + CT: a valid value → `#(ok …)`; a value with a bad field → `#(error …)` (exact) | serious | M4-5 (deferred) | open | | |
| D-2 | **Validator depth + path:** a nested ADT with a wrong-typed field *at depth* → `#(error type_error)` whose error carries a **`path`** to the failing field (e.g. `[shipped, tracking]`); a correct nested value → `#(ok …)`. | Rust/CT: nested-bad → exact error incl. path; nested-good → ok | serious | M4-5 | open | | Teaching-grade locator |
| D-3 | **HEADLINE — `decode` + web-input demo:** `(decode <type> untyped) -> #(ok T) | #(error type_error)` (graceful, NO crash). A fixture decodes an untyped term (map/tuple like parsed input) into an ADT: valid → `#(ok …)`; invalid (wrong field type / wrong tag / missing) → `#(error type_error)` with a teaching message. | CT: both paths; exact error content | serious | M4-6/M4-7 (motivating example) | open | | The 400 case; "typed user input → type error" |
| D-4 | **Guards CRASH vs validators RETURN:** the SAME bad value **crashes** when passed to a typed function head (M4 guard) but **returns** `#(error …)` through `decode`. Both correct; the distinction documented. | CT: one value, two behaviors (crash via head call; `#(error …)` via decode) | serious | M4-8 (deferred) | open | | The deliberate duality |
| D-5 | **Human render helper:** render the structured type-error Gleam-style (expected / got / path) — the runtime analog of the diagnostic engine. | Rust/CT: exact `assert_eq!` snapshot of the rendered string | serious | M4-4 caveat, design §8 | open | | Splittable → M4.6 |
| D-6 | **Error term standardized on a MAP:** the structured type-error is `#{expected, got, path, …}` (not a proplist); the **M4 guard fallback** + its tests move to the same map shape, so guards + validators share one error type. | grep: no proplist type-error; M4 guard test updated; render helper consumes the map | serious | M4 note-only | open | | Touches M4 code; keep M4 tests green |
| D-7 | **Validator cross-backend matrix:** validators correct across tagged-tuple + enum + transparent; **EXACT** assertions; native-record runtime deferred (29+). | CT matrix green (0 skipped) | serious | M4-9 (validator matrix deferred), design §9 | open | | |
| D-8 | **Full regression + standing discipline + process:** M0–M4 suites ALL pass; exact assertions (no `.contains()`); honest statuses; CT in LFE; `make check` clean; CI green (0 skipped). | full CT + Rust green; `make check` exit 0; CI green | serious | M0–M4, feedback | open | | |

## What Worked

_(Filled in at close.)_

## CDC Verification

_(Filled in by CDC against the closing SHA.)_

## Closure

_(Filled in at close. Total rows: 8.)_
