# Milestone M9.2: Expander Phase 1a (Backquote + Recursion) — Ledger

> Per LEDGER_DISCIPLINE.md. CC fills Status/Evidence (SHA + reproduced output, CI-green);
> CDC re-verifies. No row stays `open` at close. STANDING RULES
> ([[typed-test-discipline]], [[cc-editing-safety]], [[lfe-ct-tests-in-lfe]]): validate
> against the M9.1 ORACLE goldens (ground truth, exact); test the fully-plain-form path
> (not just typed bodies); preserve M0 line/position fidelity; unwired ≠ done; status
> honesty; no blind `sed`; CT in LFE. Contract: [06-expander-conventions.md](../06-expander-conventions.md).

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| B-1 | **Faithful `exp_backquote` port:** nested backquote, comma, comma-at splicing into lists/tuples/maps, cons/append optimizers — matches LFE. | Rust expander output matches `backquote.expanded` golden via the Rust↔golden harness | serious | the fragile area | | | Replaces `qq_expand` |
| B-2 | **Core-form recursion:** expansion descends into case/let/lambda/match-lambda/progn/if/try/receive/comprehensions so nested macros expand everywhere. | in-scope `core-forms` golden entries (backquote-nested) green via Rust expander | serious | structural fix | | | predef-nested entries wait for M9.3 |
| B-3 | **Run over ALL emitted forms:** expander applies to plain passthrough forms AND typed-form bodies (not just `tf.body`). Fixes the fully-plain-`defun` quasiquote gap. | CT: Duncan's M9 plain `defun`/`case` with `` `#(unix ,unsup) `` compiles+runs, binds the var (exact) | serious | M9 close follow-up | | | The root-cause fix |
| B-4 | **Rust↔golden harness:** the O-6 diff mechanism (Rust EETF → re-read → `prettyprint1` → compare golden) is implemented and runs in CI. | harness runs; `backquote.expanded` green from the Rust side | serious | regression infra | | | Grades the port vs ground truth |
| B-5 | **`qq_expand` retired:** the ad-hoc backquote/tuple code is removed; the faithful port is the only path. | grep: no `qq_expand`; tests green | normal | cleanup | | | No dead/duplicate expander |
| B-6 | **Regression + process:** full M0–M9 + M9.1 suites pass under the new expander; line/position fidelity preserved; `make check` clean; CI green (0 skipped). | full CT + Rust green; `make check` exit 0; positions intact | serious | M0–M9.1 | | | Existing fixtures still compile+run |

## CDC Verification

_(Filled in by CDC against the closing SHA.)_

## Closure

_(Filled in by CC at close: per-row walk, totals, test summary, SHA.)_
