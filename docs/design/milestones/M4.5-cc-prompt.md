# M4.5 — Claude Code implementation prompt (The Membrane)

> Paste into Claude Code from the `typed` project root. Implements M4.5 (deep validators
> + `decode` + the web-input demo) against the ledger, under ledger discipline. Builds on
> closed M0–M4.

```
You are implementing Milestone M4.5 ("The Membrane — deep validators & decode") of the
`typed` project. You are CC (implementer) under LEDGER DISCIPLINE. M0–M4 are CLOSED
(chain, ADTs, repr backends, matching/exhaustiveness, contracts, diagnostic engine, and —
M4 — always-on head guards that CRASH with a structured type-error). This milestone adds
the DEEP, GRACEFUL half: validators + decode at the untyped membrane. This is the project's
motivating example — "typed user input → type error."

# Read first (then STOP and confirm scope)
1. docs/design/milestones/M4.5-membrane.md            (model; the two faces; scope; out)
2. docs/design/milestones/M4.5-membrane-ledger.md     (criteria D-1..D-8)
3. docs/design/milestones/M4-runtime-enforcement-ledger.md (M4 guards + the structured error term it raises)
4. checker/src/{guards.rs,lower.rs,match_lower.rs,adt.rs,type_env.rs,diagnostic.rs,main.rs}
5. test/typed_runtime_SUITE.lfe (M4 runtime CT style; LFE)

# STANDING RULES (NON-NEGOTIABLE — typed-test-discipline, cc-editing-safety)
- Diagnostic/error tests assert EXACT output (assert_eq!/pattern match), never `.contains()`.
- Test EVERY backend (never assume "backend-identical").
- Unwired / cfg-test-only code is `deferred`, NOT `done`.
- A test/fixture must exercise the criterion's ACTUAL subject, not a stand-in.
- NEVER edit source with blind `sed`/in-place regex; `git checkout` to recover; `make check`
  after bulk edits.

# Ledger discipline
- Work against D-1..D-8; amendments need written justification. Fill Status + Evidence (SHA
  + reproduced output; CI green) as rows land. Per-row walk at close. Leave CDC section for
  CDC. Budget 5; if it tightens, PROPOSE SPLIT: M4.5 = validators + decode + web-demo +
  duality; M4.6 = render helper + map-standardization.

# Key design: two faces of one type
- M4 GUARDS (head, shallow): on violation → CRASH. Already done. Internal contracts.
- M4.5 VALIDATORS / decode (deep, recursive, membrane): on violation → RETURN #(error te).
  Untrusted external input handled gracefully.

# What to build
1. DEEP VALIDATORS (D-1/D-2): generate `(validate <type> term) -> #(ok term)|#(error te)`
   per declared type, RECURSIVE over constructor fields + nested ADTs; built-in base-type
   validators (integer/binary/atom/boolean/string/list/map/float). Check the carrier shape
   AND tag/arity AND recursively each field's type. On failure, build a `path` to the
   failing field (e.g. `[shipped, tracking]`). Works for tagged-tuple/enum/transparent
   (native-record runtime deferred, 29+).
2. decode (D-3): `(decode <type> untyped) -> #(ok T)|#(error te)` = run the validator at the
   boundary; NEVER crash.
3. WEB-INPUT DEMO (D-3, headline): a fixture that decodes an untyped term resembling parsed
   input (a map and/or tagged tuple) into an ADT — valid → `#(ok …)`; invalid (wrong field
   type / wrong tag / missing field) → `#(error te)` with a teaching message (the 400 case).
   Make it real; this is the milestone's point.
4. DUALITY (D-4): one bad value, two behaviors — CRASHES through a typed function head (M4
   guard) but RETURNS `#(error …)` through `decode`. Test both; document the distinction.
5. RENDER HELPER + MAP STANDARDIZATION (D-5/D-6): a helper that renders the structured
   type-error Gleam-style (expected / got / path), EXACT-snapshot tested. Standardize the
   error term on a MAP `#{expected, got, path, …}` — and MOVE the M4 guard fallback + its
   tests to the same map shape so guards and validators share one error type. Keep M4 tests
   green. (If budget tight, D-5/D-6 split to M4.6 — propose it.)
6. MATRIX (D-7): validators across tagged-tuple/enum/transparent, exact; native-record
   runtime deferred (29+).
7. REGRESSION + PROCESS (D-8): all M0–M4 suites pass; exact assertions; CT in LFE; make
   check clean; CI green, 0 skipped.

# Run & evidence
- cd checker && cargo build && cargo test; rebar3 ct; make check. Show Skipped=0.
- Commit (small, logical commits); anchor every done row's Evidence to the SHA; CI green.

# Definition of done
D-1..D-8 final with SHA + CI-green evidence (or justified deferred/no-op). `decode` turns
untyped input into `#(ok T)`/graceful `#(error te)` with a path (D-3); the web-input demo
works both ways; one bad value crashes via head / returns error via decode (D-4); the error
is a map rendered by the helper, exact-snapshot (D-5/D-6) or split-to-M4.6; validators
matrix-correct (D-7); full M0–M4 regression green. Per-row walk at close.

Do NOT expand scope: no coercion in decode, no native-record runtime, no higher-order/
message enforcement, no framework/HTTP helpers, no guard elision.
```
