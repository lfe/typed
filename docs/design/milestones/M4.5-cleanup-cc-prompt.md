# M4.5 Close-Out — Claude Code prompt (CC iteration 2)

> Paste into Claude Code from the `typed` project root. CDC verified the graceful-decode +
> duality (real!) but found the DEEP validation untested — only nullary/top-level cases are
> covered, which M4's shallow guards already catch. This pass proves the deep capability.
> Does NOT expand scope.

```
You are CC closing out Milestone M4.5 ("The Membrane"). ITERATION 2 (of 5). CDC verified
the graceful decode + the guards-vs-validators duality (D-3/D-4) are REAL. But the DEEP
part — field-type checking, path-at-depth, nesting, enum/transparent — is UNTESTED: every
fixture is nullary (`pending`) or a top-level non-match (`42`/`{bogus,1}`), all with
`path: []`. Those are cases M4's shallow guards already catch. This pass proves M4.5's
distinguishing capability. Read the ledger's "## CDC Verification" first
(docs/design/milestones/M4.5-membrane-ledger.md).

# STANDING RULES (NON-NEGOTIABLE — typed-test-discipline, cc-editing-safety)
- Tests assert EXACT output (assert_eq!/pattern match), never `.contains()`.
- Test EVERY backend (never assume backend-identical). Test the criterion's ACTUAL subject.
- Untested/unwired capability is `open`/`deferred`, NOT `done`. No blind `sed`.

# Ledger discipline
- Iteration 2 of 5. Don't expand scope. Per-row walk at close; leave CDC section intact.
- Every done row: SHA + reproduced output; CI green.

# Required corrections (add the tests that prove the DEEP validators work)

The validator code in validators.rs already generates field checks + path. The gap is
coverage. Add fixtures + CT (LFE) tests that exercise CONSTRUCTORS WITH FIELDS:

1. D-1 — with-fields validation:
   - valid: `decode-order-status({shipped, <<"TRK123">>})` → `#(ok {shipped, <<"TRK123">>})`
     (a constructor WITH a field, field type correct).
   - bad field: `decode-order-status({shipped, 42})` (tracking should be binary) →
     `#(error #(type_error …))`. Exact assertion on the error.
   (If the validator code is wrong for fields — likely, since it's never run — FIX it, no
   blind sed.)

2. D-2 — path at depth: the bad-field error above must carry a NON-EMPTY `path` pointing at
   the failing field (e.g. `[tracking]`, or `[shipped, tracking]` if you include the ctor).
   Assert the exact path. If you have a nested ADT handy, add a 2-level case asserting a
   2-element path; otherwise one level is acceptable for D-2 (note it).

3. D-3 — realistic web-input demo: the demo must decode a STRUCTURED input (a tagged tuple
   and/or a map resembling parsed JSON/form data) into an ADT — valid → `#(ok …)`,
   wrong-field → `#(error …)` with a teaching message. Not just `pending`/`42`.

4. D-7 — enum + transparent validator CT: add exact-assertion tests for an enum type
   (valid member → ok; non-member → error) and a transparent newtype (valid underlying →
   ok; wrong underlying → error). Don't assume backend-identical.

# Stays deferred → M4.6 (do NOT do here)
- D-5 render helper, D-6 map standardization (display polish). Leave `deferred`.
- native-record runtime (OTP 29+).

# Run & evidence
- cd checker && cargo build && cargo test; rebar3 ct; make check. Show Skipped=0.
- Commit; anchor D-1/D-2/D-3/D-7 to the new SHA; CI green; full M0–M4 regression green.

# Definition of a clean close
- D-1: with-fields valid + bad-field invalid, exact.
- D-2: non-empty path to the failing field, exact.
- D-3: structured (with-fields) web-input demo, both paths, exact.
- D-7: enum + transparent validator CT, exact.
- D-5/D-6 deferred to M4.6. Per-row walk; CDC section intact.

Do NOT expand scope: no render helper, no map standardization, no coercion, no
native-record runtime, no framework helpers.
```
