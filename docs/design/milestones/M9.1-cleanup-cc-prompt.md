# M9.1 Close-Out — Claude Code prompt (CC iteration 2)

> Paste into Claude Code from the `typed` project root. The oracle, harness, and conventions
> doc (O-7) are CDC-verified and strong. The corpus under-covers its criteria — and the corpus
> IS the spec for the M9.2/M9.3 port, so complete it. Read the ledger's "## CDC Verification".

```
You are CC closing out Milestone M9.1 ("Expander Oracle"). ITERATION 2 (of 5). CDC verified
O-1/O-3/O-5/O-6/O-7/O-8. O-2 and O-4 are under-covered vs their criteria. The corpus is the
SPEC the Rust port (M9.2/M9.3) will be graded against, so missing entries = forms that ship
ungraded. Read docs/design/milestones/M9.1-expander-oracle-ledger.md "## CDC Verification".

# Add the missing corpus entries, then regenerate goldens via the ORACLE
1. O-2 BACKQUOTE — MAPS: add backquoted-map entries to test/golden/corpus/backquote.lfe.txt —
   a quasiquoted map with an unquote (e.g. `` `#m(key ,v) `` or the LFE map-quasiquote form)
   and, if LFE supports it, a map splice. Regenerate the golden via `make golden-generate`;
   confirm the oracle expands it (this is ground truth — whatever the oracle emits IS correct).
2. O-4 PREDEF + def* LOWERING: add to test/golden/corpus/predef-macros.lfe.txt (or a sibling)
   entries that exercise EACH missing form named in the criterion:
   - `!==` (third comparison alias), `flet*`, `?` (receive-timeout macro), `LINE`.
   - The CL def* LOWERING family (Tier-1 pure transforms — the DEFINITION lowering, not the
     macro's later use): `defmacro`, `flet`, `fletrec`, `macrolet`, `deftype`, `defspec`.
     (Note: a defmacro's BODY being evaluated is Tier 2 / out of scope — but `defmacro`
     lowering to `define-macro` is Tier 1 and MUST be in the corpus so the port reproduces it.)
   Regenerate goldens via `make golden-generate`.
3. Fix the predef corpus header comment to match what's actually covered (it currently claims
   defmacro/LINE that were absent).

# STANDING RULES (NON-NEGOTIABLE)
- Goldens are ORACLE-PRODUCED ground truth — never hand-write expected output. Cover the ACTUAL
  subject: every form the O-2/O-4 criteria name. `make golden-verify` must pass. Status honesty
  (header comments match contents). No blind `sed`; `git checkout` to recover; `make check`
  after edits. CT in LFE.

# Ledger discipline
- Iteration 2 of 5. Don't expand scope (no Rust port; no Tier-2/3 corpus — defmacro/etc. here
  are DEFINITION lowering only, which is Tier 1). Per-row walk at close; leave the CDC section
  intact. Re-anchor O-2/O-4 to the new SHA; make check + golden-verify green; 0 skipped.

# Definition of a clean close
- O-2 covers backquoted maps; O-4 covers !==, flet*, ?, LINE, and defmacro/flet/fletrec/
  macrolet/deftype/defspec lowering — all with oracle-generated goldens; golden-verify green;
  headers honest; make check clean.

Do NOT expand scope: corpus completeness only + regenerate goldens. No Rust expander, no Tier-2
eval-time entries (only def* DEFINITION lowering, which is Tier 1).
```
