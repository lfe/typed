# M4.6 — Claude Code implementation prompt (Error rendering)

> Paste into Claude Code from the `typed` project root. Small milestone: standardize the
> runtime type-error on a map + add a render helper. Builds on closed M4 + M4.5.

```
You are implementing Milestone M4.6 ("Error term standardization & render helper") of the
`typed` project. You are CC (implementer) under LEDGER DISCIPLINE. M0–M4 + M4.5 are CLOSED
(static checking + runtime guards + deep validators/decode). This milestone finishes two
display-polish items deferred earlier. Small — aim ≤2 iterations.

# Read first (then STOP and confirm scope)
1. docs/design/milestones/M4.6-error-rendering.md          (the two items; scope)
2. docs/design/milestones/M4.6-error-rendering-ledger.md   (criteria E-1..E-5)
3. checker/src/{guards.rs,validators.rs,diagnostic.rs} + test/typed_runtime_SUITE.lfe
   (where the proplist type-error is built + matched today)

# STANDING RULES (NON-NEGOTIABLE — typed-test-discipline, cc-editing-safety)
- Exact assert_eq!/snapshots, never `.contains()`. Test the actual subject. Unwired ≠ done.
- NO blind `sed` (this milestone touches a shared shape across many tests — edit carefully,
  `git checkout` to recover, run `make check` EARLY AND OFTEN).

# Ledger discipline
- Work against E-1..E-5; amendments need written justification. Fill Status + Evidence (SHA
  + reproduced output; CI green) as rows land. Per-row walk at close; leave CDC section for CDC.

# What to build
1. E-1/E-2 MAP STANDARDIZATION: change the runtime type-error term from the proplist
   `[{expected,T},{got,V},{path,P},…]` to a MAP `#{expected => T, got => V, path => P}`
   (add `function`/`arg` keys for the guard-crash case). Emit the map from BOTH:
   - the M4 guard fallback (the crash path, in the lowering/guards code), and
   - the M4.5 validators/`decode` (the return path).
   Update EVERY test that pattern-matches the old proplist to the map shape. Keep the full
   M0–M4.5 suite green (run make check frequently).
2. E-3 RENDER HELPER: add `render-type-error/1` (an LFE function the generated/runtime code
   can call, or a small helper module) that turns the map into a Gleam-style teaching string
   — expected vs got, and the path to the failing field (e.g. "expected string at
   .shipped.tracking, got 42"). EXACT-snapshot test the rendered string (assert_eq!).
3. E-4 BOTH FACES: test the render helper on (a) a guard-CRASH error (catch it, render it)
   and (b) a validator/`decode`-RETURN error — same map shape → same render. Exact.
4. E-5: CT in LFE; make check clean; CI green, 0 skipped.

# Run & evidence
- cd checker && cargo build && cargo test; rebar3 ct; make check. Show Skipped=0.
- Commit; anchor rows to the SHA; CI green; full M0–M4.5 regression green.

# Definition of done
The runtime type-error is one map shape (E-1) used by guards + validators with all tests
migrated + green (E-2); render-type-error produces a teaching-grade string, exact-snapshot
(E-3), for both the crash and return paths (E-4); make check + CI green (E-5). Per-row walk.

Do NOT expand scope: no runtime-JSON helper, no native-record runtime, no coercion.
```
