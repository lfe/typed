# M3.5 — Claude Code implementation prompt (Cleanup)

> Paste into Claude Code from the `typed` project root. Finishes M3's four deferrals.
> Consolidation milestone — should close in ≤2 iterations. Does NOT expand scope.

```
You are implementing Milestone M3.5 ("Cleanup — finish M3's deferrals") of the `typed`
project. You are CC (implementer) under LEDGER DISCIPLINE. M0/M1/M2/M3 are CLOSED (chain,
ADTs, matching/exhaustiveness, the diagnostic engine, bidirectional contract checking).
This milestone finishes the four things M3 honestly deferred — consolidation, not new
territory.

# Read first (then STOP and confirm scope)
1. docs/design/milestones/M3.5-cleanup.md           (scope; the four deferrals; out)
2. docs/design/milestones/M3.5-cleanup-ledger.md    (criteria C-1..C-8)
3. docs/design/milestones/M3-contracts-ledger.md    (the CDC Verification section = what was deferred & why)
4. checker/src/{typecheck.rs,matching.rs,match_lower.rs,diagnostic.rs,adt.rs,type_env.rs,main.rs}
5. README.md (the order-status + describe example to make real)

# STANDING RULES in force (memory: typed-test-discipline) — NON-NEGOTIABLE
- Diagnostic tests assert the EXACT rendered output (assert_eq! / golden snapshot),
  NEVER `.contains()` / substring.
- Every backend/representation claim is TESTED, never assumed.
- Absent capability is `deferred`, not `done`.

# Ledger discipline
- Work against C-1..C-8; amendments need written justification. Fill Status + Evidence
  (SHA + reproduced output; CI green) as rows land. Per-row walk at close. Leave the CDC
  section for CDC. Budget 5 (aim ≤2); if C-5 threatens it, propose splitting C-5 to M3.6.

# What to build
1. C-1 ENGINE ROUTING: route M3 type errors (body-vs-:returns, call-arg, wrong-arity,
   field-value) through diagnostic::DiagnosticCollector so they render with span + caret
   + `--> file:line:col` + hint, in human AND JSON — like the M2 exhaustiveness errors.
   Remove the ad-hoc eprintln! path for TYPE errors (CLI/IO eprintln! may stay). Add
   EXACT golden snapshots (assert_eq! full render) for each, human + JSON.
2. C-2 BRANCH-BODY TYPING: make case/typed stop synthesizing Dynamic. In check mode
   (expected type known), check each clause body against the expected type; in synth
   mode, synth each branch and require agreement; synthesize the match result type.
   A wrong-typed branch → exact diagnostic. Reuse M2's matching/exhaustiveness (already
   guarantees completeness).
3. C-3 CASE-AS-BODY: a defun/typed whose body is a case/typed checks against :returns
   (every branch must match :returns). Accept matching; reject a divergent branch (exact).
4. C-4 README DEMO: add fixtures for the README order-status deftype + describe — the
   CORRECT constructor version (passes) and the WRONG string-pattern version (rejected).
   CT (LFE) asserting both, with the rejection's exact diagnostic. Keep the README's
   example and the fixture in sync.
5. C-5 (should) BASIC POLY: a single-level type-var unifier — `(:args ((x a)(y a)))
   (:returns a)` binds `a` from the first arg, checks the second is the same, returns it;
   identity accepted; same-var-different-types rejected (exact). NOT full HM; no nested/
   higher-rank. If this threatens the budget, STOP and propose C-5 → M3.6.
6. C-6/C-7/C-8: apply the standing discipline to all new tests; keep M0–M3 + matrix green
   (full regression, line injection holds); CT in LFE; make check clean; CI green, 0 skipped.

# Run & evidence
- cd checker && cargo build && cargo test; rebar3 ct; make check. Show Skipped=0.
- Commit; anchor every done row's Evidence to the SHA; confirm CI green.

# Definition of done
C-1..C-8 final with SHA + CI-green evidence (or justified deferred/no-op). All diagnostics
render through the engine (human+JSON, exact snapshots); case/typed branch typing rejects
a wrong branch; the README describe example type-checks (correct)/is rejected (strings);
basic poly contracts check consistently (or C-5 split to M3.6 with rationale). Full M0–M3
regression green. Per-row walk at close.

Do NOT expand scope: no full HM, no nested generics, no runtime enforcement (M4), no
nested exhaustiveness.
```
