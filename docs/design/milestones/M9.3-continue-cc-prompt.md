# M9.3 — Claude Code CONTINUE prompt (iteration 2)

> Paste into Claude Code from the `typed` project root. Iteration 1 (`f332aa8`) did P-8 (plain
> defun — proven) + P-9, and PART of P-4. CDC decision: CONTINUE in M9.3 (no follow-up split).
> Finish the predef table + complete P-4 + land the P-7 validation gate. Read the ledger's
> "## CDC Verification" first.

```
You are continuing Milestone M9.3 ("Expander Phase 1b") of the `typed` project. ITERATION 2
(of 5). CDC verified P-8 + P-9 done; P-4 is PARTIAL; P-1/P-2/P-3/P-5/P-6/P-7 remain. Continue
in this milestone (no follow-up). Read docs/design/milestones/M9.3-expander-predef-ledger.md
"## CDC Verification".

# Priorities (sequence matters)
1. STAND UP P-7 HARNESS INFRASTRUCTURE EARLY so you can validate-as-you-go. Implement the
   Rust↔golden diff (Rust expander -> EETF -> Erlang harness re-reads -> prettyprint1 -> compare
   committed golden), wired into make/CI. Even before all goldens pass, get the mechanism
   running so each predef row below turns its golden green incrementally.
2. COMPLETE P-4 (the missing def* lowering): flet->let-function, fletrec->letrec-function,
   macrolet, deftype->define-type, defspec->define-function-spec. (defun/defmodule/defmacro/cond
   already done.) Match the oracle lowering exactly.
3. P-1: c*r (caar..cddddr) + comparison aliases (!=/===/!== -> /=/=:=/=/=).
4. P-2: control/binding sugar: list*, let*->nested let, flet*, do->letrec-function loop,
   fun/2,3->gensym arg lists, ?->receive-timeout.
5. P-3: MODULE/LINE + colon-call sugar ((mod:fun ...)/m:f -> (call 'mod 'fun ...)).
6. P-5: defrecord/defstruct generation (make-/is-/match-/set-/getters/setters).
7. P-6: gensym byte-fidelity (|-N-| vc, do$^N fc, exact counter threading) — verify against the
   do/fun goldens.

# THE GATE (P-7 definition of success)
ALL 4 committed goldens green FROM THE RUST SIDE via the harness:
  backquote.expanded (RETROACTIVELY validates M9.2's port), core-forms.expanded,
  predef-macros.expanded, defrecord.expanded.
If the golden-diff exposes a fidelity gap in M9.2's backquote port, FIX it — the oracle is
ground truth.

# STANDING RULES (NON-NEGOTIABLE)
- The golden-diff is the REAL verification, not compile+run alone. Match the oracle EXACTLY
  (gensym names byte-for-byte). Status honesty: a def* form is "done" only when ALL its named
  forms are handled (don't mark P-4 done with forms missing again). Unwired ≠ done. No blind
  `sed`; `git checkout`; `make check` after edits. CT in LFE. Preserve M0 positions.

# Ledger discipline
- Iteration 2 of 5. If P-7 or gensym fidelity proves harder than expected and you approach the
  cap, say so explicitly (don't silently drop rows). Per-row walk at close; leave CDC section
  for CDC. Anchor done rows to the SHA.

# Definition of done (M9.3 close)
P-1..P-6 complete (predef table + def* + defrecord + gensym byte-exact); P-7 gate green — ALL 4
goldens match from the Rust side (incl. retroactive backquote); P-8/P-9 already done; full
regression green, positions intact. Then Tier-1 expansion is COMPLETE and oracle-validated.

Do NOT expand scope: NO Tier-2 (eval-time user macros / eval-when-compile) — only def*
DEFINITION lowering; NO Tier-3 (.hrl/QLC/imported).
```
