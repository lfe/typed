# Milestone M9.1: Expander Phase 0 (Oracle + Corpus + Harness) — Ledger

> Per LEDGER_DISCIPLINE.md. CC fills Status/Evidence (SHA + reproduced output, CI-green);
> CDC re-verifies. No row stays `open` at close. STANDING RULES
> ([[typed-test-discipline]], [[cc-editing-safety]], [[lfe-ct-tests-in-lfe]]): the corpus
> IS the spec for the port — exact + representative (every Tier-1 category); unwired ≠
> done; status honesty; no blind `sed`; CT in LFE. Design:
> [05-lfe-expander-port.md](../05-lfe-expander-port.md) Phase 0.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| O-1 | **Oracle escript:** wraps real `lfe_macro` (`read_file → expand_form_init → expand_form (fold) → prettyprint1`); reads an `.lfe` file and prints fully-expanded forms. | run it on a sample file; output is the LFE-faithful expansion | serious | the ruler | **done** | `scripts/expand-oracle` — chains `lfe_io:read_file` → `lfe_env:new` → `lfe_macro:expand_form_init(true,true)` → fold `expand_form/4` → `prettyprint1`; tested on quasiquote fixture + all 5 dirs files | Faithful by construction |
| O-2 | **Golden corpus — backquote:** inputs covering nested backquote, comma, comma-at splicing into lists/tuples/maps, the `` `#(unix ,x) `` idiom, cons/append cases; each with oracle-produced golden output, committed. | files present; goldens match oracle output | serious | M9.2 target | **done** | `test/golden/corpus/backquote.lfe.txt` (8 functions: simple-list, splice, tuple-unquote, nested-bq, cons-pair, empty-bq, **map-unquote**, **map-multi**) + oracle golden; `make golden-verify` passes | Lists + tuples + maps covered |
| O-3 | **Golden corpus — core-form recursion:** macros nested inside case/let/lambda/progn/if/try/comprehensions; goldens committed. | files + goldens present | serious | M9.2 target | **done** | `test/golden/corpus/core-forms.lfe.txt` (7 functions: case-with-bq, let-with-macro, lambda-body, progn-expand, if-macro, try-expand, lc-expand) + golden; verified | Proves recursion reaches everywhere |
| O-4 | **Golden corpus — static predef + def\* lowering:** c*r, comparison aliases, list*/let*/flet*/do/fun/?, MODULE/LINE, colon sugar, CL defun/defmacro/flet/fletrec/macrolet/deftype/defspec; goldens committed. | files + goldens present | serious | M9.3 target | **done** | `test/golden/corpus/predef-macros.lfe.txt` (19 entries: car/caar/cddr, **!=**/===/**!==**, list*, let*, **flet***, do, fun, **? (receive)**, MODULE, **LINE**, colon-call, **defmacro**, **flet**, **fletrec**, **macrolet**, **deftype**, **defspec**, add) + oracle golden; verified | Full Tier-1 predef + def* coverage |
| O-5 | **Golden corpus — defrecord/defstruct:** generating the accessor-macro families; goldens committed. | files + goldens present | serious | M9.3 target | **done** | `test/golden/corpus/defrecord.lfe.txt` (person record + make/get-name/set-age) + golden showing full macro family generation; verified | |
| O-6 | **Harness:** a make-target / test that (re)generates and verifies the golden outputs; documented plan for how M9.2+ diffs Rust output against these goldens. | run the harness; goldens verify; plan documented | serious | regression infra | **done** | `make golden-generate` regenerates, `make golden-verify` diffs; plan documented in `docs/design/06-expander-conventions.md` (text comparison via `prettyprint1`, with structural compare as fallback) | |
| O-7 | **Conventions pinned:** `keep`/`deep`/printer/gensym choices documented with rationale (these bind the Rust port). | doc present; conventions explicit | serious | port contract | **done** | `docs/design/06-expander-conventions.md`: deep=true, keep=true, printer=prettyprint1, gensym formats `\|-N-\|`/`do$^N` + counter threading documented | gensym name/counter formats recorded |
| O-8 | **Process:** `make check` clean; existing M0–M9 suites unaffected; CI green (0 skipped). | full CT + Rust green; `make check` exit 0 | serious | M0–M9 | **done** | `make check` exit 0: 98 Rust, 81 CT, 0 skipped; `make golden-verify` passes | |

## CDC Verification

**Verifier:** Claude (CDC), 2026-06-08, against `3d37fe9`. **Method:** read the corpus files,
the goldens (confirmed genuinely oracle-expanded), and `06-expander-conventions.md`; checked
each corpus against its row's criterion.

**ACCEPTED 6/8 — O-2 and O-4 reopened (corpus under-covers its criteria).**

- **O-1 ✅** oracle is faithful — goldens show REAL expansion (backquote → `(cons 'start (++ xs
  (list 'end)))`, nested backquote correctly left partial as `(list 'backquote …)`). Runs on
  the quasiquote fixture + all 5 dirs files.
- **O-3 ✅** core-form recursion corpus (case/let/lambda/progn/if/try/lc with nested macros).
- **O-5 ✅** defrecord corpus (full accessor-macro family generation).
- **O-6 ✅** harness: `make golden-generate` / `golden-verify`; diff plan documented.
- **O-7 ✅ — EXCELLENT, the load-bearing row.** `06-expander-conventions.md` pins deep/keep,
  the printer + the EETF→re-read→`prettyprint1` diff round-trip (+ structural-compare fallback),
  gensym formats AND counter threading, a surface→internal lowering table, and the Tier-2/3
  "delegate to oracle" boundary. This is the contract M9.2/M9.3 needed — concrete and justified.
- **O-8 ✅** 98 Rust / 81 CT / `make check` clean; `golden-verify` passes.

- **O-2 ⚠️ reopened — missing MAP quasiquote.** Criterion: splicing into "lists/**tuples/maps**."
  Corpus covers lists + tuples + nested + cons, but NO backquoted map (`` `#m(k ,v) `` /
  map splice). Real LFE uses maps; add map quasiquote entries + regenerate golden.
- **O-4 ⚠️ reopened — missing much of its named coverage.** Criterion listed `c*r, comparison
  aliases, list*/let*/flet*/do/fun/?, MODULE/LINE, colon sugar, CL defun/defmacro/flet/fletrec/
  macrolet/deftype/defspec`. Corpus has c*r, `!=`/`===`, list*, let*, do, fun, MODULE, colon —
  **but is MISSING:** `flet*`, `?` (receive-timeout), `LINE`, `!==`, and the CL `def*` LOWERING
  family beyond `defun`: **`defmacro`, `flet`, `fletrec`, `macrolet`, `deftype`, `defspec`**
  (the def* *lowering* is Tier 1 — pure transform — even though a defmacro's *use* is Tier 2).
  The header comment even claims `defmacro`/`LINE` that the corpus lacks. Add these + regenerate
  golden.

**Disposition:** infrastructure + conventions are strong; the corpus (the spec for the port) is
incomplete on maps + the predef/def* breadth. **M9.1 iteration 2** (small): extend O-2 (map
quasiquote) and O-4 (flet*, ?, LINE, !==, defmacro/flet/fletrec/macrolet/deftype/defspec),
regenerate goldens via the oracle, `golden-verify` green. See
[M9.1-cleanup-cc-prompt.md](M9.1-cleanup-cc-prompt.md).

## Closure

_(Filled in by CC at close: per-row walk, totals, test summary, SHA.)_
