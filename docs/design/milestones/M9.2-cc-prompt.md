# M9.2 — Claude Code implementation prompt (Expander Phase 1a: Backquote + Recursion)

> Paste into Claude Code from the `typed` project root. Port LFE's backquote expander + the
> core-form recursion to Rust, validated against the M9.1 oracle goldens, and run expansion
> over ALL emitted forms. Builds on closed M9.1.

```
You are implementing Milestone M9.2 ("Expander Phase 1a: Backquote + Core-Form Recursion") of
the `typed` project. You are CC (implementer) under LEDGER DISCIPLINE. M0–M9 + M9.1 are CLOSED.
You are porting the FIRST slice of LFE's Tier-1 macro expander to Rust.

# Read first (then STOP and confirm scope)
1. docs/design/milestones/M9.2-expander-backquote.md        (scope)
2. docs/design/milestones/M9.2-expander-backquote-ledger.md (criteria B-1..B-6)
3. docs/design/06-expander-conventions.md (THE BINDING CONTRACT — knobs, printer, diff round-
   trip, gensym formats, surface→internal lowering table)
4. The M9.1 oracle + goldens: scripts/expand-oracle, test/golden/ (backquote.* + core-forms.*)
5. LFE source for fidelity: lfe_macro.erl exp_backquote/2 + exp_bq_append/exp_bq_cons (the
   algorithm to match); the exp_form cascade (recursion structure)
6. checker/src/lower.rs (the current ad-hoc qq_expand you are REPLACING) + main.rs:280 (where
   expand_quasiquotes is called ONLY on tf.body — you will run expansion over ALL forms)

# STANDING RULES (NON-NEGOTIABLE)
- Validate against the M9.1 ORACLE goldens — ground truth, exact. Never hand-write expected
  output. TEST THE ACTUAL SUBJECT: the fully-plain-`defun`/`case` path (not just typed bodies);
  the M0 line/position fidelity MUST survive (this is why we expand in Rust, not via lfe_macro).
  Unwired ≠ done. Status honesty. No blind `sed`; `git checkout` to recover; `make check` after
  edits. CT in LFE.

# What to build (each row validated against goldens / exact CT)
1. B-1 BACKQUOTE PORT: implement a faithful exp_backquote in Rust — nested backquote, comma,
   comma-at splicing into LISTS, TUPLES, and MAPS, with the cons/append optimizers — matching
   lfe_macro's algorithm. The Rust expander's output must match test/golden/expected/
   backquote.expanded via the Rust↔golden harness (B-4).
2. B-2 CORE-FORM RECURSION: port the exp_form structural recursion so expansion descends into
   case/let/lambda/match-lambda/progn/if/try/receive/comprehensions — a nested backquote
   expands anywhere. The in-scope core-forms golden entries (backquote-nested) go green via the
   Rust expander. (Entries whose nested macro is a PREDEF wait for M9.3 — don't force them.)
3. B-3 RUN OVER ALL FORMS: apply the expander to EVERY form emitted to lfe_codegen — plain
   passthrough defun/case AND typed-form bodies — not just tf.body (main.rs:280). CT: Duncan's
   exact plain example compiles+runs and binds the unquoted var:
     (defun test (os-tuple)
       (case os-tuple
         (#(unix linux) 'linux)
         (`#(unix ,unsup) unsup)
         (_ 'other)))
   assert classify #(unix freebsd) -> 'freebsd. (This is the root-cause fix.)
4. B-4 RUST↔GOLDEN HARNESS: implement the O-6 diff mechanism (Rust expander emits EETF -> an
   Erlang harness re-reads -> prettyprint1 -> compare against the committed golden). Wire it
   into CI. backquote.expanded green from the RUST side.
5. B-5 RETIRE qq_expand: remove the ad-hoc backquote/tuple code; the faithful port is the only
   path. grep shows no qq_expand remains.
6. B-6 REGRESSION: full M0–M9 + M9.1 green under the new expander; line/position fidelity
   preserved (diagnostics still point at the right line); make check clean; CI green, 0 skipped.

# Ledger discipline
- Work B-1..B-6. Budget 5 iterations. Discovered fidelity gaps vs the oracle -> fix to match
  (the oracle is ground truth), or a deferred row with rationale if genuinely out of Tier-1
  scope. Per-row walk at close; leave CDC section for CDC. Anchor done rows to the SHA.

# Definition of done
Faithful Rust exp_backquote matching backquote.expanded via the harness (B-1/B-4); core-form
recursion reaches nested backquotes (B-2); expansion runs over ALL forms — plain example
compiles+runs (B-3); qq_expand removed (B-5); full regression green, positions intact (B-6).
Per-row walk at close.

Do NOT expand scope: NO predef table / defrecord generation (M9.3); no Tier-2/3. Just
backquote + recursion + run-over-all-forms + the diff harness + retiring qq_expand.
```
