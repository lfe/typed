# Milestone M3.5: Cleanup (Finish M3's Deferrals)

> Per LEDGER_DISCIPLINE.md. CC fills Status/Evidence (commit SHA + reproduced output,
> CI-green); CDC re-verifies. No row stays `open` at close. STANDING RULES in force
> ([[typed-test-discipline]]): exact diagnostics (never `.contains()`), test every
> backend, honest status. Should close in ≤2 iterations.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| C-1 | **Engine routing:** M3 type errors (body-vs-`:returns`, call-arg, wrong-arity, field-value) render through the M2 `DiagnosticCollector` — span + caret + `--> file:line:col` + hint — in both human and JSON. The ad-hoc `eprintln!` path for *type* errors is removed. | Rust + snapshot: each M3 error renders with span+caret (human) + JSON; exact `assert_eq!` snapshots | serious | M3-10 (deferred), design §8 | open | | Reuses the M2 engine |
| C-2 | **`case/typed` branch-body typing:** each clause body is checked against the expected result type; the match synthesizes a result type; a wrong-typed branch is REJECTED with an exact diagnostic. (No longer synthesizes `Dynamic`.) | Rust + CT: wrong-typed branch rejected (exact snapshot); well-typed match accepted; result type correct | serious | M3-6 (deferred), M2 | open | | Integrates M2 exhaustiveness + M3 :returns |
| C-3 | **`case/typed` as a function body** checks against `:returns`: a `defun/typed` whose body is a `case/typed` is accepted iff every branch matches `:returns`. | Rust + CT: matching case-body accepted; a branch ≠ `:returns` rejected (exact) | serious | M3-3 + C-2 | open | | The M3+M2 fusion |
| C-4 | **README `describe` demo:** the README `order-status` deftype + the *correct* (constructor) `describe` type-checks clean; the *wrong* (string-pattern) version is REJECTED. | CT/CLI: correct describe passes; strings version rejected (exact diagnostic) | serious | M3-11 (deferred), README | open | | Dogfoods the public example; integration test for C-1..C-3 |
| C-5 | **Basic polymorphic contracts:** type variables in `:args`/`:returns` checked consistently — `(:args ((x a) (y a))) (:returns a)` requires both args the same type and returns it; identity accepted; inconsistent use rejected. | Rust + CT: identity accepted; same-var-different-types rejected (exact) | should | M3-14 (deferred), design §6 | open | | Single-level unifier; full HM out. Split to M3.6 if it threatens the budget |
| C-6 | **Standing discipline applied:** all new diagnostic tests assert EXACT output (no `.contains()` on messages); JSON variants where relevant; honest statuses. | grep new tests for `.contains(` on messages (should be none); review | serious | [[typed-test-discipline]] | open | | The trend fix, enforced |
| C-7 | **Full regression + line injection:** M0/M1/M2/M3 suites ALL still pass; line injection holds; matrix green. | full CT + Rust green (0 skipped); a line-injection assertion | serious | M0–M3 | open | | |
| C-8 | **Process:** CT in LFE (`*_SUITE.lfe`); `make check` clean (clippy -D, rustfmt, xref); CI green (0 skipped). | CI green; `make check` exit 0 | polish | feedback | open | | |

## What Worked

_(Filled in at close.)_

## CDC Verification

_(Filled in by CDC against the closing SHA.)_

## Closure

_(Filled in at close. Total rows: 8.)_
