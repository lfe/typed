# M9.3 — Claude Code prompt: clean the P-7 validation gate (iteration 3)

> Paste into Claude Code from the `typed` project root. P-1..P-6/P-8/P-9 are CDC-accepted. P-7
> (the gate) is reopened: it's "1/4 green with explained diffs," but the headline gate must be
> OBJECTIVELY clean. The root cause is a corpus-scoping miss (Tier-2 USES in a Tier-1 corpus),
> not a port bug. Read the ledger's "## CDC Verification — full M9.3" first.

```
You are closing the P-7 validation gate of Milestone M9.3. ITERATION 3 (of 5). P-1..P-6, P-8,
P-9 are accepted. P-7 is NOT cleanly closed. Read docs/design/milestones/
M9.3-expander-predef-ledger.md "## CDC Verification — full M9.3".

# The bar: the Tier-1 gate must be 100% GREEN, Tier-2 explicitly QUARANTINED
"Green except for diffs we explain" is not acceptable for the headline gate. Make it objective:
every Tier-1 corpus entry matches the oracle from the Rust side; every Tier-2 entry is moved out
of the Tier-1 gate and documented as "expand via oracle/delegate — not graded here."

# Do this
1. PARTITION THE CORPUS by tier:
   - Tier-1 (MUST be green): static rewrites (c*r, comparison aliases, list*/let*/flet*/do/fun/
     ?), MODULE/LINE, colon sugar, def* LOWERING (defun/defmodule/defmacro/flet/fletrec/macrolet/
     deftype/defspec → their define-* forms), defrecord/defstruct GENERATION (the emitted macro
     family), and ALL backquote.
   - Tier-2 (QUARANTINE — move to a clearly-labeled Tier-2 corpus/section, NOT in the Tier-1
     gate): any USE of a user/record/macrolet macro — e.g. `(make-person …)`, `(person-name p)`,
     `(set-person-age …)`, and the macrolet body that USES its local macro. These require
     `lfe_eval` (Tier 2) and are out of scope; the gate must not depend on them. Keep them as
     documented "delegate to oracle" examples for the future Tier-2 milestone.
   Rebuild the goldens so the Tier-1 corpus contains only Tier-1 forms (defrecord corpus tests
   GENERATION, not use; macrolet/defmacro test LOWERING, not use).
2. REPRESENTATION diffs → STRUCTURAL COMPARE (already decided for maps): compare expanded forms
   as TERMS (re-read both sides), ignoring map key/iteration order and line annotations, per
   06-expander-conventions.md's documented fallback. Apply it so map-order and line-anno
   differences don't fail the gate. backquote.expanded → GREEN.
3. ADJUDICATE the two unclassified diffs WITH THE ACTUAL DIFF SHOWN (don't assert):
   - `defmacro BQ preservation`: defmacro LOWERING is Tier-1. Show the oracle vs Rust output for
     `(defmacro my-const () `42)`. If the Rust port mishandles the body's backquote, FIX it to
     match the oracle. If it's a representation diff, handle via structural compare.
   - `LINE counter`: show the diff. If (LINE) should expand to the real source line and the Rust
     port differs, FIX; if it's a line-anno representation difference, structural compare covers
     it.
4. The gate result must be: Tier-1 goldens ALL GREEN from the Rust side via the harness;
   quarantined Tier-2 entries listed in the ledger/conventions doc with rationale.

# STANDING RULES (NON-NEGOTIABLE)
- The gate is OBJECTIVE: green on Tier-1, not "explained." Show actual diffs when classifying;
  never hand-wave a diff as "doesn't count" without demonstrating its tier. Structural compare
  is the documented tool for representation diffs — use it, don't chase byte-order. Status
  honesty. No blind `sed`; `git checkout`; `make check` + `make golden-rust-verify` green. CT
  in LFE.

# Ledger discipline
- Iteration 3 of 5. If adjudicating defmacro-BQ/LINE reveals a real Tier-1 fidelity bug, fixing
  it is in scope (the oracle is ground truth). Per-row walk at close; leave CDC section intact.
  Re-anchor P-7 to the SHA.

# Definition of a clean close (P-7)
Tier-1 goldens 100% green from the Rust side (backquote + core-forms + Tier-1 predef + defrecord
generation); Tier-2 uses quarantined + documented; defmacro-BQ and LINE adjudicated (fixed or
structurally-compared with the diff shown); make check + golden-rust-verify green; 0 skipped.

Do NOT expand scope: do NOT implement Tier-2 eval to make macro-uses expand (that's a future
milestone) — quarantine them. Just make the Tier-1 gate objectively green + partition + adjudicate.
```
```
