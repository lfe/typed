# M9.1 — Claude Code implementation prompt (Expander Phase 0: Oracle + Corpus + Harness)

> Paste into Claude Code from the `typed` project root. Build the faithfulness ruler for the
> Tier-1 expander port: an Erlang oracle wrapping the real `lfe_macro`, a golden corpus, a
> harness, and pinned conventions. Builds on closed M0–M9. NO Rust expander code here.

```
You are implementing Milestone M9.1 ("Expander Phase 0: Oracle + Corpus + Harness") of the
`typed` project. You are CC (implementer) under LEDGER DISCIPLINE. M0–M9 are CLOSED. This
milestone builds INFRASTRUCTURE ONLY — no Rust port (that's M9.2/M9.3).

# Context (why this exists)
The typed driver runs lfe_lint + lfe_codegen + compile:forms — NO lfe_macro. So passed-through
forms get NO macro expansion; backquote was the first symptom (M9 D-4), and cond/let*/do/
defrecord break the same way. We will port LFE's Tier-1 expander to Rust next. First we need an
ORACLE that produces the exact expansion LFE itself would, to grade the port against.

# Read first (then STOP and confirm scope)
1. docs/design/milestones/M9.1-expander-oracle.md        (scope; conventions to pin)
2. docs/design/milestones/M9.1-expander-oracle-ledger.md (criteria O-1..O-8)
3. docs/design/05-lfe-expander-port.md (the full review — §7 sketches the oracle escript)
4. The LFE source for the API: lfe_io:read_file/1, lfe_env:new/0,
   lfe_macro:expand_form_init/2, lfe_macro:expand_form/4, lfe_io:print1/1
   (LFE 2.2.1 is a project dep; use its ebin)

# STANDING RULES (NON-NEGOTIABLE)
- The CORPUS IS THE SPEC for M9.2/M9.3 — make it exact and representative: cover EVERY Tier-1
  category (not a sample). Goldens must be produced BY THE ORACLE (ground truth), committed.
  Unwired ≠ done. Status honesty. No blind `sed`; `git checkout` to recover; `make check`
  after edits. CT in LFE.

# What to build (each row gets evidence)
1. O-1 ORACLE: an Erlang escript (e.g. scripts/expand-oracle or priv/) that chains
   lfe_io:read_file -> lfe_env:new -> lfe_macro:expand_form_init -> fold lfe_macro:expand_form
   (threading Env + #mac{}) -> lfe_io:print1, reading a file (and/or stdin), printing the
   fully-expanded forms. Run it on a sample; show faithful output.
2. O-2 CORPUS backquote: inputs covering nested backquote, comma, comma-at splicing into
   LISTS, TUPLES, and MAPS, the `` `#(unix ,x) `` tuple idiom, and cons/append cases. Commit
   each input + its oracle golden output.
3. O-3 CORPUS core-form recursion: macros nested inside case/let/lambda/progn/if/try/lc/bc.
   Commit inputs + goldens.
4. O-4 CORPUS static predef + def* lowering: c*r (caar..cddddr), comparison aliases
   (!=/===/!==), list*/let*/flet*/do/fun/?, MODULE/LINE, colon-call sugar, CL defun/defmacro/
   flet/fletrec/macrolet/deftype/defspec. Commit inputs + goldens.
5. O-5 CORPUS defrecord/defstruct: generating the accessor-macro families. Commit inputs +
   goldens.
6. O-6 HARNESS: a make-target (e.g. `make expander-golden` / a CT or script) that regenerates
   AND verifies the goldens against the oracle. Document (in the conventions note) HOW M9.2+
   will diff the Rust expander's output against these goldens (structural compare vs. print1
   text — pick one).
7. O-7 CONVENTIONS: document keep/deep/printer/gensym choices + rationale (these BIND the Rust
   port). Record the exact gensym name formats (|-N-|, do$^N) and counter behavior.
8. O-8 PROCESS: make check clean; M0–M9 suites unaffected; CI green, 0 skipped.

# Ledger discipline
- Work O-1..O-8. Budget 5 iterations. Per-row walk at close; leave CDC section for CDC. Anchor
  done rows to the SHA; CI green.

# Definition of done
Oracle expands LFE files via real lfe_macro (O-1); golden corpus covers ALL Tier-1 categories
with oracle goldens (O-2..O-5); harness regenerates/verifies + diff plan documented (O-6);
conventions pinned (O-7); make check clean (O-8). Per-row walk at close.

Do NOT expand scope: NO Rust expander code (M9.2/M9.3); NO Tier-2 (eval-time macros) or Tier-3
(.hrl/QLC/imported) corpus entries — stay within Tier 1. Just the oracle, the Tier-1 corpus,
the harness, and the pinned conventions.
```
