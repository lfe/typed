# Milestone M4.6: Error Term Standardization & Render Helper

> Per LEDGER_DISCIPLINE.md. CC fills Status/Evidence (SHA + reproduced output, CI-green);
> CDC re-verifies. No row stays `open` at close. STANDING RULES ([[typed-test-discipline]],
> [[cc-editing-safety]]): exact `assert_eq!`/snapshots; test every backend; unwired ≠ done;
> test the actual subject; no blind `sed`. This changes a SHARED error shape — run
> `make check` early/often.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| E-1 | **Error term is a MAP:** the runtime type-error is `#{expected => T, got => V, path => P, …}` (optional `function`/`arg` for the guard-crash case), used by BOTH the M4 guard fallback and the M4.5 validators/`decode`. No proplist type-error remains. | grep: no `[{expected` proplist error; both paths emit the map | serious | M4 note, M4.5 D-6 | open | | One shape for both faces |
| E-2 | **Migration green:** every existing test that matched the proplist error is updated to the map shape; M4 (guard crash) + M4.5 (validator/decode) suites pass; full M0–M4.5 regression green. | full CT + Rust green (0 skipped) | serious | M0–M4.5 | open | | The shared-shape refactor |
| E-3 | **Render helper:** `(render-type-error TE) -> string` prints the error Gleam-style — expected / got / path-to-failing-field. | Rust/CT: exact `assert_eq!` snapshot of the rendered string | serious | M4-4 caveat, M4.5 D-5, design §8 | open | | Runtime analog of the diagnostic engine |
| E-4 | **Render works for BOTH faces:** the same helper renders a guard-CRASH error and a validator/`decode`-RETURN error (same map → same render). | CT: render a crash error + a decode error; both teaching-grade (exact) | serious | M4.5 D-4 | open | | |
| E-5 | **Process:** CT in LFE; `make check` clean (clippy -D, rustfmt, xref); CI green (0 skipped). | CI green; `make check` exit 0 | polish | feedback | open | | |

## What Worked

_(Filled in at close.)_

## CDC Verification

_(Filled in by CDC against the closing SHA.)_

## Closure

_(Filled in at close. Total rows: 5.)_
