# M3.5 Close-Out — Claude Code prompt (CC iteration 2)

> Paste into Claude Code from the `typed` project root. CDC confirmed the corruption was
> cleanly recovered (0 lines lost) and branch-body typing is real — but three items need
> work. Does NOT expand scope.

```
You are CC closing out Milestone M3.5. ITERATION 2 (of 5). CDC verified: typecheck.rs
recovered cleanly from the sed corruption (+143/-0, nothing lost); branch-body typing
(C-2/C-3) is real and exact-tested. Three corrections remain. Read the ledger's
"## CDC Verification" section first (docs/design/milestones/M3.5-cleanup-ledger.md).

# STANDING RULES (memory: typed-test-discipline) — NON-NEGOTIABLE
- Diagnostic tests assert EXACT output (assert_eq!/snapshot), never `.contains()`.
- A test/fixture must exercise the CRITERION'S ACTUAL SUBJECT, not a stand-in.
- Unwired / dead / #[cfg(test)]-only code is NOT `done` — it's `deferred`.

# Ledger discipline
- Iteration 2 of 5. Don't expand scope. Amendments need written justification.
- Every done row: SHA + reproduced output; CI green. Per-row walk at close. Leave CDC
  section intact.

# Required corrections

1. C-4 → make the fixture ACTUALLY the README example (this is the milestone's point).
   The current describe_good.tlfe is a variant (Delivered instead of Cancelled, atom
   returns instead of string `++`). Replace it (and describe_bad.tlfe) with the EXACT
   README example:
     (deftype order-status (Pending) (Shipped (tracking string)) (Cancelled (reason string)))
     correct describe: :returns string; branches "queued" / (++ "on its way: " t) / (++ "cancelled: " r)
     wrong describe: matches string patterns ("pending" ...) → rejected
   Then make the checker TYPE-CHECK the correct version clean. This likely needs:
     - string literals synth as `string` ([char]);
     - `++` typed so `(++ string string)` → string (string is a list, so reconcile the
       List/String relationship in types_compatible OR give `++` a string-aware result).
   Keep README and fixture IN SYNC. If making `++`/string work is genuinely more than this
   close-out can hold, STOP and propose: either (a) M3.6 for `++`/string typing, or
   (b) reconcile the README to a form that type-checks today — and say which, with rationale.
   Either way, no "variant labeled as the README demo".

2. C-1 → add an EXACT render snapshot. Routing is wired; now snapshot an M3 error
   RENDERED through DiagnosticCollector (span + caret + `--> file:line:col` + hint) in BOTH
   human and JSON (assert_eq! full output, à la m2_11). At least the return-type mismatch.

3. C-5 → either WIRE IT or mark `deferred` (CDC already set `deferred`). If wiring: call
   check_poly_contract from main.rs for contracts whose :args/:returns use type variables,
   drop `#[cfg(test)]`, and add a CT/Rust test proving a real poly contract is checked
   end-to-end. If not wiring this pass: leave `deferred` with a re-entry note. (It's a
   "should" — deferral is fine; just be honest.)

# Run & evidence
- cd checker && cargo build && cargo test; rebar3 ct; make check. Show Skipped=0.
- Commit; anchor changed rows to the new SHA; confirm CI green.

# Definition of a clean close
- C-4: fixture == README example, correct version type-checks, wrong version rejected
  (exact diagnostic); README ⇄ fixture in sync. (Or an explicit, justified reconciliation.)
- C-1: exact span+caret render snapshot (human + JSON) for an M3 error.
- C-5: wired + tested end-to-end, OR `deferred` with re-entry.
- Per-row walk complete; full M0–M3 regression green; CDC section intact.

Do NOT expand scope: no full HM, no nested generics, no runtime enforcement (M4).
```
