# M9.3 — Claude Code implementation prompt (Expander Phase 1b: Predef + Records + Gate)

> Paste into Claude Code from the `typed` project root. Complete the Tier-1 expander port (the
> static predef table + defrecord/defstruct + gensym fidelity) AND stand up the Rust↔golden
> harness so EVERY oracle golden goes green from the Rust side. Builds on closed M9.1 + M9.2.

```
You are implementing Milestone M9.3 ("Expander Phase 1b: Predef + Records + Validation Gate") of
the `typed` project. You are CC (implementer) under LEDGER DISCIPLINE. M0–M9, M9.1, M9.2 CLOSED.
This COMPLETES the Tier-1 expander port and PROVES it against the oracle goldens.

# Read first (then STOP and confirm scope)
1. docs/design/milestones/M9.3-expander-predef.md        (scope; why this is the gate)
2. docs/design/milestones/M9.3-expander-predef-ledger.md (criteria P-1..P-9)
3. docs/design/06-expander-conventions.md (THE CONTRACT — gensym formats, lowering table,
   diff round-trip)
4. test/golden/ (the 4 corpora + goldens — ground truth) + scripts/expand-oracle
5. LFE source: lfe_macro.erl exp_predef/3 (the predef table to port), lfe_macro_record.erl /
   lfe_macro_struct.erl (record/struct generation), new_symb/new_fun_name (gensym formats)
6. checker/src/expander.rs (M9.2's backquote+recursion — you extend it) + main.rs

# STANDING RULES (NON-NEGOTIABLE)
- The ORACLE goldens are GROUND TRUTH. The golden-diff (P-7) is the REAL verification — NOT
  compile+run alone. Match the oracle EXACTLY (gensym names byte-for-byte). TEST THE ACTUAL
  SUBJECT: the fully-plain `defun` (P-8), not just defun/typed. Preserve M0 line/position
  fidelity. Unwired ≠ done. Status honesty. No blind `sed`; `git checkout`; `make check` after
  edits. CT in LFE.

# What to build (each row green via the golden harness / exact CT)
1. P-1 c*r (caar..cddddr) + comparison aliases (!=/===/!== → /=/=:=/=/=).
2. P-2 control/binding sugar: list*, let*→nested let, flet*, do→letrec-function loop, fun/2,3→
   gensym arg lists, ?→receive-timeout. (do/fun need P-6 gensym fidelity.)
3. P-3 MODULE/LINE + colon-call sugar ((mod:fun ...)/m:f → (call 'mod 'fun ...)).
4. P-4 CL def* LOWERING (definition transform only — NOT macro eval): defmodule→define-module,
   defun→define-function, defmacro→define-macro, flet→let-function, fletrec→letrec-function,
   macrolet, deftype→define-type, defspec→define-function-spec. Match the oracle lowering.
5. P-5 defrecord/defstruct generation: the full accessor-macro family; defrecord.expanded green.
6. P-6 GENSYM byte-fidelity: |-N-| (vc), do$^N (fc), exact counter threading — match the oracle
   byte-for-byte (verify against the do/fun goldens).
7. P-7 VALIDATION GATE (carried from M9.2 B-4): implement the Rust↔golden harness — Rust
   expander emits EETF -> Erlang harness re-reads -> prettyprint1 -> compare against the
   committed golden. Turn ALL 4 goldens green FROM THE RUST SIDE: backquote.expanded
   (retroactively validating M9.2's port), core-forms.expanded, predef-macros.expanded,
   defrecord.expanded. Wire into CI/make. THIS IS THE HEADLINE — it's where fidelity is proven.
8. P-8 (carried B-3): add a FULLY-PLAIN `(defun … (case … `` `#(unix ,x) `` …))` fixture (NOT
   defun/typed) that compiles+runs and binds the var. CT exact. If it doesn't work end-to-end,
   FIX it (this is the actual plain-form subject that's been slipping).
9. P-9 REGRESSION: full M0–M9.2 green; positions intact; make check clean; CI green, 0 skipped.

# Ledger discipline
- Work P-1..P-9. Budget 5 iterations. If the golden-diff (P-7) exposes a fidelity gap in the
  M9.2 backquote port, FIX it (the oracle is ground truth). Discovered out-of-Tier-1 forms ->
  deferred row with rationale. Per-row walk at close; leave CDC section for CDC.

# Definition of done
Predef table + def* lowering + defrecord/defstruct ported with byte-exact gensyms; ALL 4 oracle
goldens green from the Rust side via the harness (P-7 — incl. retroactive backquote); the
fully-plain defun backquote case compiles+runs (P-8); full regression green, positions intact.
Per-row walk at close. After this, Tier-1 expansion is COMPLETE and oracle-validated.

Do NOT expand scope: NO Tier-2 (eval-time user macros, eval-when-compile) — only def*
DEFINITION lowering; NO Tier-3 (.hrl/QLC/imported). Those stay behind the delegate boundary.
```
